# Tmux Setup

## Steps

1. Added `tmux.conf` file to dotfiles/tmux directory then symlink to ~/.config/tmux/ using [`mklink`](../mklink) script.
2. Set `C-Space` to prefix
3. Install Tmux Plugin Manager [tpm](https://github.com/tmux-plugins/tpm) at `~/.tmux/plugins/tpm`

